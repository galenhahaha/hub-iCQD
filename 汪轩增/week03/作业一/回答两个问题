1、 langchain 工具调用 和 llm function call 有什么区别？
llm function call 是模型底层能力，大模型只负责理解语义并输出一份“该调什么函数、传什么参数”的 JSON 计划书，自己绝不运行任何代码；langchain 工具调用是上层工程框架，它不仅帮模型自动生成这份 JSON 计划，还会真正帮模型去调用 API/代码、拿到结果，再把结果塞回给模型进行下一轮思考，把整个自动化闭环给彻底跑通。
2、 langchain 工具调用 的 速度是受到什么影响？
大模型生成 Function Call 的响应速度（通常占大头）、外部 API 或本地代码本身的执行耗时、给模型塞了太多的工具定义导致上下文变长，以及多个工具是在串行排队还是并发执行。
