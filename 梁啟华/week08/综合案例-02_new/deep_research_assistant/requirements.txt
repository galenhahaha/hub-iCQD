# ============================================================
# 深度研究助手 · 运行依赖
# 建议 Python 3.10+
# 安装：pip install -r requirements.txt
# ============================================================

# Web 框架：提供异步路由 / 请求校验 / OpenAPI 文档
fastapi>=0.110,<1.0

# ASGI 服务器：本地启动用
uvicorn[standard]>=0.29

# 异步 HTTP 客户端：用于调 Bocha 搜索接口、抓取网页、请求大模型
httpx>=0.27

# 读取 .env 文件（可选，未安装则自动跳过加载）
python-dotenv>=1.0

# pydantic 由 fastapi 自动携带（v2），此处显式声明便于单独使用
pydantic>=2.6,<3.0
