assets 静态资源目录（数据、文档）
------------------------------------------------------------------------------------------
assets/
├─ dataset/
│  ├─ baidu_stopwords.txt    # 停用词表；停用词过滤（如cn_stopwords）可以去除对意图识别没有帮助的常见词汇，从而提高模型效果。
│  └─ dataset.csv            # 测试原始数据集，包含【文本-标签】
├─ doc/
│  ├─ 01_项目背景文档.md     # 项目背景、项目要求、项目产出
│  ├─ 02_项目实施文档.md     # 技术方案的选择、数据集的准备，数据集的评测标准、编码（后端开发、算法开发） 、实施、测试和部署、精度问题，实施中可能会遇到的问题和解决方案
│  ├─ 03_项目运维文档.md     # 部署前准备，测试/生产环境启动命令、API接口文档
│  └─ 04_项目面试点.md       # 关于项目实现的面试点
└─ logs/
   └─ tfevents日志文件       # TensorBoard训练可视化日志，模型训练时生成，用于查看loss、精度曲线
------------------------------------------------------------------------------------------
model 推理模块
model/
├─ bert.py          # 使用 bert的业务逻辑代码
├─ prompt.py        # 使用大语言模型的业务逻辑代码
├─ regex_rule.py    # 使用正则表达式的业务逻辑代码
└─ tfidf_ml.py      # 使用TF-IDF方法的业务逻辑代码
------------------------------------------------------------------------------------------
test/
└─ data.json        # 测试样本JSON
------------------------------------------------------------------------------------------
training_code/
├─ train_bert.py    # BERT模型训练脚本：读取dataset.csv、微调预训练模型、保存权重
└─ train_tfidf.py   # TF-IDF+机器学习模型训练脚本：训练完成导出 tfidf_ml.pkl 模型文件
------------------------------------------------------------------------------------------
app.log              # 项目运行日志文件
config.py            # 最重要的配置文件
data_schema.py       # Pydantic数据模型：定义接口请求体、响应体结构
fastapi_demp.py      # FastAPI Demo测试文件；早期接口原型测试
logger.py            # 统一日志格式、日志级别、输出文件/控制台
main.py              # 项目入口！FastAPI主程序；uvicorn启动入口，注册路由、加载模型、启动HTTP服务
README.md            # 代码流程、压测服务、接口、部署

=====================
运行 main.py，读取 config.py 获取全局配置
通过生命周期事件，提前加载：
model/bert.py：加载 BERT 逻辑
model/prompt.py:加载大模型逻辑
model/regex_rule.py：加载正则规则
model/tfidf_ml.py: tfidf逻辑

单次 HTTP 请求完整链路
客户端发起 POST 请求到达main.py中定义的接口路由
参数不合法 → 直接返回错误日志 + 报错 JSON
参数校验通过，进入业务逻辑
封装标准返回 JSON，发送给客户端