回答如下问题： 

        1、 langchain 工具调用 和 llm function call 有什么区别？ 

Llm function call是帮我们选择工具并且得到这个工具的调用方式 

Langchain不仅帮我们选择了工具，还帮我们执行了工具，最后还会对最终的结果进行汇总(tool选择，tool执行，tool总结) 

        2、 langchain 工具调用 的 速度是受到什么影响？ 

 Langchain的速度受到workflow叠加的影响(大模型思考耗时，两次网络请求耗时，工具本身干活耗时） 

 

作业2: 阅读意图识别 01-intent-classify 代码，梳理源文件的作用，绘制从fastapi 接受请求到返回结果的流程，绘制流程图（手绘、自然语言表达）。 
客户端发一个 POST 请求，里面带着文本，比如「帮我播放周杰伦的歌曲」。请求会打到 main.py 里四个接口里的某一个：regex、tfidf、bert 或者 gpt。客户端自己决定调哪个地址，代码里没有统一的分发器去帮你选。 

FastAPI 收到请求后，先用 data_schema.py 里的 Pydantic 模型检查字段合不合法。不合法直接报错返回，合法就进入对应的处理函数。 

进入函数后，先记一下开始时间，再准备一个空的返回结构。有的接口会顺便打条日志。然后调用对应的模型：调正则，就看句子里有没有配置好的关键词，有就返回对应类别，没有就归到 Other。调 TF-IDF，就先分词去停用词，再算向量，用训练好的 SVM 预测类别。调 BERT，就先编码，过一遍模型，取概率最大的那个类别。调大模型，就先找训练集里最像的十条当例子，拼成提示词，发给通义千问，让它直接吐出类别。 

模型跑完后，把结果填进返回结构，顺便算一下花了多少时间。如果中间出错了，就把错误信息写进去。最后 FastAPI 把这个结构转成 JSON 还给客户端。 
