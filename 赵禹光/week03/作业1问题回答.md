1、LangChain 工具调用和 LLM function call 的区别？
  LangChain 工具调用本质上就是对 LLM function call 的一层封装。不用手写 JSON Schema。@tool 装饰器自动把函数签名转成模型能懂的格式；也不用手动把工具结果拼回消息列表，invoke() 帮你一步搞定。

2、LangChain 工具调用的速度受什么影响？
    api调用次数、调用模式：流式体感上更快、模型体量：小模型快，大模型慢。
