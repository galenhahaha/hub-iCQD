# Environment variable reference only; the application does not load .env files.
# Inject these variables into the service process through your deployment platform.
DEEPSEEK_API_KEY=
# OPENAI_API_KEY is also supported as a fallback.
OPENAI_BASE_URL=https://api.deepseek.com/
MODEL_NAME=deepseek-v4-flash

BOCHA_API_KEY=
DEEPSEEK_API_KEY=
BOCHA_SEARCH_COUNT=10
RESEARCH_MAX_ROUNDS=3
LLM_RETRIES=3

FRONTEND_ORIGIN=http://localhost:3000

# Configure the listening address using uvicorn --host and --port.
