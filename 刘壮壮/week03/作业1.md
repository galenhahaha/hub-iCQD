### 1、 langchain 工具调用 和 llm function call 有什么区别？
LangChain 工具调用建立在模型原生 function/tool calling 之上：负责工具定义与 schema 转换、绑定、解析 tool_calls、执行工具并把结果回传；
### 2、 langchain 工具调用 的 速度是受到什么影响？
速度主要受多轮大模型推理和网络延时影响，其次是工具自身耗时与调用轮数/是否并行；
